---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: checkers.checker.cloudification.io
spec:
  group: checker.cloudification.io
  names:
    kind: Checker
    listKind: CheckerList
    plural: checkers
    singular: checker
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The current target status
      jsonPath: .status.targetStatus
      name: Target Status
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: Checker is the Schema for the checkers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CheckerSpec defines the desired state of Checker
            properties:
              targetUrl:
                description: Foo is an example field of Checker. Edit checker_types.go
                  to remove/update
                type: string
            required:
            - targetUrl
            type: object
          status:
            description: CheckerStatus defines the observed state of Checker
            properties:
              targetStatus:
                type: string
            required:
            - targetStatus
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
